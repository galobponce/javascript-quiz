import JsLogo from "/Javascript.svg"

export const JavascriptLogo = ({
  width,
  height,
}: {
  width: number
  height: number
}) => <img src={JsLogo} width={width} height={height} alt="Javascript Logo" />
